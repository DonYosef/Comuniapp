# Variables de entorno globales para el monorepo
# Copia este archivo a .env y configura los valores

# API Backend URL (usado por apps/web)
NEXT_PUBLIC_API_URL=http://localhost:3001

# Variables específicas por app:
# - apps/api/.env: Variables del backend (Flow, DB, etc.)
# - apps/web/.env.local: Variables locales del frontend (opcional)
