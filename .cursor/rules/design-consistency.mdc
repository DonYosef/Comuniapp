# Reglas de Consistencia de Diseño - Comuniapp

## 🎨 Principios de Diseño Moderno

### 📱 Principio Mobile-First (OBLIGATORIO)

#### **Breakpoints Responsive**
```css
/* Tailwind CSS - Mobile First */
sm: 640px   /* Small devices (landscape phones) */
md: 768px   /* Medium devices (tablets) */
lg: 1024px  /* Large devices (desktops) */
xl: 1280px  /* Extra large devices */
2xl: 1536px /* 2X large devices */
```

#### **Estrategia Mobile-First**
1. **Diseño base**: Optimizado para móvil (< 640px)
2. **Progresión**: Mejoras incrementales para pantallas más grandes
3. **Touch-friendly**: Elementos mínimos de 44px para interacción táctil
4. **Contenido prioritario**: Información esencial visible en móvil

### Contenedor Principal
- **Siempre usar**: `className="space-y-6 sm:space-y-8 animate-fade-in"` como contenedor principal
- **Nunca usar**: `min-h-screen bg-gray-50` o contenedores con `max-w-*` fijos
- **Estructura**: `<div className="space-y-6 sm:space-y-8 animate-fade-in">` como wrapper principal

### Header Moderno Mobile-First (Obligatorio)
```tsx
{/* Header moderno */}
<div className="bg-gradient-to-r from-[color]-50 to-[color]-50 dark:from-gray-800 dark:to-gray-900 rounded-2xl p-4 sm:p-6 md:p-8 border border-[color]-100 dark:border-gray-700 animate-slide-down">
  <div className="flex flex-col sm:flex-row sm:items-center sm:justify-between gap-4 sm:gap-6">
    <div className="flex items-center space-x-3">
      <div className="p-2 sm:p-3 bg-[color]-100 dark:bg-[color]-900 rounded-xl">
        <svg className="w-6 h-6 sm:w-8 sm:h-8 text-[color]-600 dark:text-[color]-400" fill="none" stroke="currentColor" viewBox="0 0 24 24">
          {/* Icono SVG apropiado */}
        </svg>
      </div>
      <div>
        <h1 className="text-2xl sm:text-3xl font-bold text-gray-900 dark:text-white">
          Título Principal
        </h1>
        <p className="text-sm sm:text-base lg:text-lg text-gray-600 dark:text-gray-400">
          Subtítulo descriptivo
        </p>
      </div>
    </div>
    {/* Botones de acción aquí si son necesarios */}
    <div className="flex flex-col sm:flex-row gap-2 sm:gap-3">
      {/* Botones con texto adaptativo */}
    </div>
  </div>
</div>
```

### Cards/Secciones (Obligatorio)
```tsx
{/* Nombre de la Sección */}
<div className="bg-white dark:bg-gray-800 rounded-2xl shadow-lg border border-gray-100 dark:border-gray-700 overflow-hidden animate-fade-in-up" style={{animationDelay: '0.Xs'}}>
  <div className="bg-gradient-to-r from-[color]-50 to-[color]-50 dark:from-[color]-900/20 dark:to-[color]-900/20 px-8 py-6 border-b border-gray-200 dark:border-gray-700">
    <h2 className="text-2xl font-bold text-gray-900 dark:text-white flex items-center">
      <div className="p-2 bg-[color]-100 dark:bg-[color]-900 rounded-lg mr-3">
        <svg className="w-6 h-6 text-[color]-600 dark:text-[color]-400" fill="none" stroke="currentColor" viewBox="0 0 24 24">
          {/* Icono SVG apropiado */}
        </svg>
      </div>
      Título de la Sección
    </h2>
    <p className="text-gray-600 dark:text-gray-400 mt-2">
      Descripción de la sección
    </p>
  </div>
  
  <div className="p-6">
    {/* Contenido de la sección */}
  </div>
</div>
```

## 🎯 Paleta de Colores por Contexto

### Colores por Tipo de Vista
- **Gestión de Residentes**: `blue` (azul/índigo)
- **Crear Comunidad**: `green` (verde/esmeralda)  
- **Editar Comunidad**: `blue` (azul/índigo)
- **Configuración**: `purple` (púrpura/índigo)
- **Reportes**: `orange` (naranja/rosa)
- **Administración**: `red` (rojo/rosa)

### Aplicación de Colores
```tsx
// Header
bg-gradient-to-r from-[color]-50 to-[color]-50
border border-[color]-100

// Icono del header
bg-[color]-100 dark:bg-[color]-900
text-[color]-600 dark:text-[color]-400

// Header de sección
from-[color]-50 to-[color]-50 dark:from-[color]-900/20 dark:to-[color]-900/20

// Icono de sección
bg-[color]-100 dark:bg-[color]-900
text-[color]-600 dark:text-[color]-400
```

## ⚡ Animaciones Obligatorias

### Delays de Animación (Escalonados)
- **Header**: Sin delay (aparece primero)
- **Primera sección**: `animationDelay: '0.1s'`
- **Segunda sección**: `animationDelay: '0.2s'`
- **Tercera sección**: `animationDelay: '0.3s'`
- **Cuarta sección**: `animationDelay: '0.4s'`
- **Quinta sección**: `animationDelay: '0.5s'`

### Clases de Animación
- **Contenedor principal**: `animate-fade-in`
- **Header**: `animate-slide-down`
- **Secciones**: `animate-fade-in-up`

## 📏 Espaciado y Dimensiones Mobile-First

### Espaciado Obligatorio Responsive
- **Entre secciones**: `space-y-6 sm:space-y-8`
- **Header padding**: `p-4 sm:p-6 md:p-8`
- **Sección header**: `px-6 sm:px-8 py-4 sm:py-6`
- **Contenido de sección**: `p-4 sm:p-6`
- **Gap en headers**: `gap-4 sm:gap-6`
- **Gap en botones**: `gap-2 sm:gap-3`

### Bordes y Sombras Responsive
- **Bordes redondeados**: `rounded-xl sm:rounded-2xl` (mobile-first)
- **Sombras**: `shadow-lg` (nunca `shadow-md` o `shadow-sm`)
- **Bordes**: `border border-gray-100 dark:border-gray-700`

## 📊 Métricas y Tablas Mobile-First

### Métricas Responsive (Obligatorio)
```tsx
{/* Grid de métricas */}
<div className="grid grid-cols-2 lg:grid-cols-4 gap-3 sm:gap-4 md:gap-6">
  <div className="bg-white dark:bg-gray-800 rounded-xl sm:rounded-2xl shadow-lg border border-gray-100 dark:border-gray-700 overflow-hidden transition-all duration-300 hover:-translate-y-1">
    <div className="p-4 sm:p-6">
      <div className="flex items-center space-x-3 sm:space-x-4">
        <div className="p-2 sm:p-3 bg-gradient-to-br from-[color]-500 to-[color]-600 rounded-lg sm:rounded-xl shadow-lg">
          <svg className="w-5 h-5 sm:w-7 sm:h-7 text-white" fill="none" stroke="currentColor" viewBox="0 0 24 24">
            {/* Icono */}
          </svg>
        </div>
        <div>
          <p className="text-xs sm:text-sm font-medium text-gray-600 dark:text-gray-400">Etiqueta</p>
          <p className="text-xl sm:text-2xl lg:text-3xl font-bold text-gray-900 dark:text-white">Valor</p>
        </div>
      </div>
    </div>
  </div>
</div>
```

### Tablas con Vista Mobile (Obligatorio)
```tsx
{/* Vista Desktop - Tabla */}
<div className="hidden lg:block overflow-x-auto">
  <table className="min-w-full divide-y divide-gray-200 dark:divide-gray-700">
    {/* Tabla tradicional */}
  </table>
</div>

{/* Vista Mobile - Cards */}
<div className="lg:hidden space-y-4">
  {items.map((item) => (
    <div key={item.id} className="bg-white dark:bg-gray-800 rounded-xl shadow-lg border border-gray-100 dark:border-gray-700 p-4">
      {/* Información organizada en cards compactas */}
      <div className="grid grid-cols-2 gap-3 mb-3">
        <div>
          <p className="text-xs font-medium text-gray-500 dark:text-gray-400 mb-1">Campo 1</p>
          <p className="text-sm text-gray-900 dark:text-white">Valor 1</p>
        </div>
        <div>
          <p className="text-xs font-medium text-gray-500 dark:text-gray-400 mb-1">Campo 2</p>
          <p className="text-sm text-gray-900 dark:text-white">Valor 2</p>
        </div>
      </div>
      {/* Botones de acción con padding táctil */}
      <div className="flex justify-end space-x-2">
        <button className="p-3 text-gray-400 hover:text-blue-600 hover:bg-blue-50 dark:hover:bg-blue-900/20 rounded-lg transition-all duration-200">
          {/* Icono */}
        </button>
      </div>
    </div>
  ))}
</div>
```

## 🔧 Estructura de Archivos

### Orden Obligatorio en el JSX (Mobile-First)
```tsx
<ProtectedRoute>
  <DashboardLayout>
    <div className="space-y-6 sm:space-y-8 animate-fade-in">
      {/* 1. Header moderno mobile-first */}
      
      {/* 2. Métricas responsive (si aplica) */}
      
      {/* 3. Secciones con animaciones escalonadas */}
      
      {/* 4. Tablas con vista dual (desktop/mobile) */}
      
      {/* 5. Modales (si los hay) */}
    </div>
  </DashboardLayout>
</ProtectedRoute>
```

### Imports de Estilos
- **Siempre incluir**: Las animaciones CSS están en `globals.css`
- **Verificar**: Que las clases de animación estén disponibles

## 🚫 Prohibiciones Estrictas

### NO Hacer
- ❌ Usar `min-h-screen bg-gray-50` como contenedor principal
- ❌ Usar `max-w-*` con valores fijos en el contenedor principal
- ❌ Usar `rounded-lg` en lugar de `rounded-2xl`
- ❌ Usar `shadow-md` o `shadow-sm` en lugar de `shadow-lg`
- ❌ Usar `px-6 py-4` en headers de sección (usar `px-8 py-6`)
- ❌ Usar `text-xl` en headers de sección (usar `text-2xl`)
- ❌ Usar `mb-8` o `mb-6` entre secciones (usar `space-y-8`)
- ❌ Usar iconos emoji en lugar de SVG
- ❌ Usar gradientes de texto (usar colores sólidos)

### SIEMPRE Hacer (Mobile-First)
- ✅ Usar `space-y-6 sm:space-y-8 animate-fade-in` como contenedor principal
- ✅ Usar `rounded-xl sm:rounded-2xl` para bordes redondeados
- ✅ Usar `shadow-lg` para sombras
- ✅ Usar `p-4 sm:p-6 md:p-8` en headers
- ✅ Usar `text-2xl sm:text-3xl` para títulos principales
- ✅ Usar `text-xl sm:text-2xl` para títulos de sección
- ✅ Usar SVG icons en contenedores redondeados
- ✅ Usar animaciones escalonadas con delays
- ✅ Usar gradientes sutiles en fondos de headers
- ✅ Usar `grid-cols-2 lg:grid-cols-4` para métricas
- ✅ Implementar vista dual para tablas (desktop/mobile)

## 📱 Responsive Design

### Breakpoints Obligatorios
- **Mobile**: `flex-col` en headers
- **Desktop**: `lg:flex-row` en headers
- **Padding adaptativo**: `p-6 md:p-8`
- **Grid responsivo**: `grid-cols-1 md:grid-cols-2 lg:grid-cols-4`

## 🎨 Ejemplo Completo de Vista

```tsx
export default function NuevaVistaPage() {
  return (
    <ProtectedRoute>
      <DashboardLayout>
        <div className="space-y-8 animate-fade-in">
          {/* Header moderno */}
          <div className="bg-gradient-to-r from-blue-50 to-indigo-50 dark:from-gray-800 dark:to-gray-900 rounded-2xl p-6 md:p-8 border border-blue-100 dark:border-gray-700 animate-slide-down">
            <div className="flex flex-col lg:flex-row lg:items-center lg:justify-between gap-6">
              <div className="space-y-2">
                <div className="flex items-center space-x-3">
                  <div className="p-3 bg-blue-100 dark:bg-blue-900 rounded-xl">
                    <svg className="w-8 h-8 text-blue-600 dark:text-blue-400" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                      {/* Icono apropiado */}
                    </svg>
                  </div>
                  <div>
                    <h1 className="text-3xl font-bold text-gray-900 dark:text-white">
                      Título de la Vista
                    </h1>
                    <p className="text-gray-600 dark:text-gray-400 text-lg">
                      Descripción de la vista
                    </p>
                  </div>
                </div>
              </div>
            </div>
          </div>

          {/* Sección 1 */}
          <div className="bg-white dark:bg-gray-800 rounded-2xl shadow-lg border border-gray-100 dark:border-gray-700 overflow-hidden animate-fade-in-up" style={{animationDelay: '0.1s'}}>
            <div className="bg-gradient-to-r from-green-50 to-emerald-50 dark:from-green-900/20 dark:to-emerald-900/20 px-8 py-6 border-b border-gray-200 dark:border-gray-700">
              <h2 className="text-2xl font-bold text-gray-900 dark:text-white flex items-center">
                <div className="p-2 bg-green-100 dark:bg-green-900 rounded-lg mr-3">
                  <svg className="w-6 h-6 text-green-600 dark:text-green-400" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                    {/* Icono apropiado */}
                  </svg>
                </div>
                Título de la Sección
              </h2>
              <p className="text-gray-600 dark:text-gray-400 mt-2">
                Descripción de la sección
              </p>
            </div>
            <div className="p-6">
              {/* Contenido */}
            </div>
          </div>

          {/* Más secciones con delays incrementales... */}
        </div>
      </DashboardLayout>
    </ProtectedRoute>
  );
}
```

## 🔍 Checklist de Verificación

Antes de considerar una vista "terminada", verificar:

- [ ] ¿Usa `space-y-6 sm:space-y-8 animate-fade-in` como contenedor principal?
- [ ] ¿Tiene header mobile-first con gradiente y animación `animate-slide-down`?
- [ ] ¿Todas las secciones usan `rounded-xl sm:rounded-2xl` y `shadow-lg`?
- [ ] ¿Los headers usan `p-4 sm:p-6 md:p-8` y `text-2xl sm:text-3xl`?
- [ ] ¿Las animaciones tienen delays escalonados (0.1s, 0.2s, etc.)?
- [ ] ¿Los iconos son SVG en contenedores redondeados?
- [ ] ¿Los colores siguen la paleta definida para el contexto?
- [ ] ¿Es responsive mobile-first con `flex-col sm:flex-row`?
- [ ] ¿Las métricas usan `grid-cols-2 lg:grid-cols-4`?
- [ ] ¿Las tablas tienen vista dual (desktop/mobile)?
- [ ] ¿Los elementos tienen tamaño táctil mínimo (44px)?
- [ ] ¿No hay scroll horizontal no deseado en móvil?
- [ ] ¿No hay errores de linting?
- [ ] ¿La estructura sigue el orden obligatorio mobile-first?

---

**Nota**: Esta regla debe aplicarse a TODAS las vistas nuevas y modificaciones de vistas existentes para mantener la consistencia visual en todo el proyecto Comuniapp.